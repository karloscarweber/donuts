# frozen_string_literal: true
source 'https://rubygems.org'

# gem 'camping', git: 'https://github.com/karloscarweber/camping', branch: 'camping-3' # This is gonna be changed to the release version of camping when it is released.
# gem 'rake'
gem 'kdl' # maybe drop kdl as a dependency for config settings and just stick to ruby. Would be easier.
gem 'camping', github: 'karloscarweber/camping', branch: 'camping-3'
gem 'phlex'
gem 'roda' # maybe don't need this'
gem 'rackup' # also maybe don't need this, comes with Camping.
gem 'green_dots', git: 'https://github.com/joeldrapper/green_dots'
# gem 'falcon'
# gem 'tipi'

group :production do
end

group :development do
end

group :test do
end

